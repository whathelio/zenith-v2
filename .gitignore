# Python virtual environment
.venv/
venv/
env/
envs/

# Python bytecode
__pycache__/
.pytest_cache/
*.py[cod]
*.pyo
*.egg-info/

# Node / Frontend
frontend/node_modules/
frontend/.vite/
frontend/tsconfig.tsbuildinfo

# Frontend build artifacts are KEPT (dist/) so users can run without Node.js

# User data
data/*.db
data/*.sqlite
data/.deps_installed
data/logs/
data/distill/
data/code_temp/
data/audit/

# Config (may contain API keys)
config/config.yaml
config/mcp_overrides.json

# Environment secrets
.env

# 脱敏映射表（{{SEC_xxx}} ↔ 明文，绝不上传）
tools/secure_map.json
tools/*.map.json

# 临时/调试文件
out.txt
_aux*.py

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Session cache
.zenith.browser

# Daily plans (personal)
data/daily_plans/

# Knowledge base data (should not be in this repo)
zenith_rag/
zenith_wiki/
chroma_db/
tasks.db

# 临时探测脚本 / 本地快捷方式
_probe*.py
*.lnk

# 数据库备份（含敏感数据，绝不上传）
data/backup/
data/*.bak*
data/*.testbak
data/*.db.*

# 启动调试日志
_pw_debug*.txt

# 前端调试测试产物
frontend/_*.cjs

# 通用备份文件
*.bak

# 本地归档目录（清理时移动的旧文件，不入库）
_archive_cleanup_20260825/


# 运行时外部归档（服务生成的归档输出，不入库）
data/archive_external/
