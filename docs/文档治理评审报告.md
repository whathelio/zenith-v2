# 文档治理评审报告：zenith-v2

> 评审目标：`O:/下载文件/新建文件夹/zenith-v2`
> 评审时间：2026-07-18
> 角色：第三方只读文档治理评审员
> 原则：只扫描、不修改、不移动、不提交任何文件

---

## 一、事实核查表

| 核查项 | 值 | 状态 |
|--------|----|----|
| `currentVersion`（backend/app.py / package.json） | `2.0.0` | `[已核实事实]` |
| 跟踪文件数（根目录下 md/json/js/ts/py/yaml，排除 .venv / node_modules / dist） | 33 | `[已核实事实]` |
| md 文件数（项目自身文档，排除依赖） | 2 | `[已核实事实]` |
| NOTES 数量 | 0 | `[已核实事实]` |
| README 版本条目数 | 0（README 无显式版本历史条目） | `[已核实事实]` |
| CHANGELOG 大小 | 无 `CHANGELOG.md` / `CHANGELOG.json` | `[已核实事实]` |
| 关键断言文件（sc-layers.js 等） | 无 | `[已核实事实]` |
| 关键字符串组1命中（README / README_VERSION_HISTORY） | 仅 `README.md` | `[已核实事实]` |
| 关键字符串组2命中（未改范围 / 不改变 / 不改 / 导入导出格式 / 恢复点 / 底层地图数据 / 坐标） | 0 | `[已核实事实]` |
| 关键字符串组3命中（CHANGELOG.json / CHANGELOG.md） | 0 | `[已核实事实]` |
| `AGENTS.md` | 无 | `[已核实事实]` |
| 市场分析功能当前状态 | `config.py:57` 设置为 `market_analysis_enabled: False`，标注“已封存” | `[已核实事实]` |
| 前端 `MarketView` | 不存在 | `[已核实事实]` |
| README 项目结构图中列出的文件 | 全部存在 | `[已核实事实]` |

---

## 二、压缩候选表（含影响范围与风险）

| 行号 | 版本号 | 内容摘要 | 是否被断言 | 断言行号 | 影响范围 | 风险级别 | 回滚预案 | 建议处理 | 理由 |
|------|--------|----------|-----------|----------|----------|----------|----------|----------|------|
| README.md:13 | 2.0.0 | “市场分析 — CFTC 持仓 + 宏观指标 + 事件 → LLM 日度报告（每日 07:00 自动运行）” | 否 | 无 | 功能入口描述 | 🟡 | 直接改回描述或删除此行 | 标注为“已封存”或删除 | 该功能在 `config.py:57` 已标记为已封存，默认关闭，前端无入口，README 描述已过时 |
| docs/百尺目标板块移植评估.md | 2.0.0 | 百尺目标板块移植评估报告 | 否 | 无 | 临时决策文档 | 🟢 | 直接删除或归档 | 移入 `docs/archive/` 或删除 | 移植已完成（`database.py` + `GoalsView.tsx`），该文档为一次性评估中间产物，无持续维护价值 |
| README.md:59-86 | 2.0.0 | 项目结构树 | 否 | 无 | 新用户 onboarding | 🟢 | 直接修改 | 保留但精简 | 文件列表全部存在，但可用表格或更紧凑格式替代；非承重内容 |
| README.md:88-94 | 2.0.0 | 技术栈说明 | 否 | 无 | onboarding | 🟢 | 直接修改 | 保留 | 内容准确，但可合并到快速开始部分 |
| README.md:17-57 | 2.0.0 | 快速开始（安装、配置、启动、构建） | 否 | 无 | onboarding | 🟢 | 直接修改 | 保留 | 启动命令 `python start.py 8766` 与 `start.py` 一致；`npm run build` 与 `package.json` 一致；内容基本可用 |

**风险级别说明**：
- 🔴 高：不可逆操作 / 被代码硬依赖 / 承重内容
- 🟡 中：可回滚但需验证 / 间接依赖
- 🟢 低：安全压缩 / 纯历史记录

---

## 三、最小安全第一刀方案

### 入口压缩
- README 中过时的“市场分析”描述（第 13 行）可以安全修改为“已封存”或删除。
- 项目结构树（第 59-86 行）可保留，但建议从“树状图”改为“紧凑表格”，减少竖向空间。

### 保留承重内容
- 无被断言代码引用的文档句段。README 中所有文件路径均为描述性，非硬编码依赖。
- 启动命令、端口、构建脚本等已被 `start.py` 和 `package.json` 实际使用，README 中应一字不差保留以维护 onboarding 一致性。

### 版本号状态
- 当前版本 `2.0.0`（`backend/app.py:240`、`backend/app.py:293`、`frontend/package.json:4`）。
- 本次文档压缩不涉及功能变更，**不推进版本更新**。

### 治理线检查
- 无 `DOC-*` 文档治理标记。建议新增 `DOC-001` 标记：README 市场分析描述需与实际功能状态同步。

### 护城河同步
- 无断言文件需要升级校验范围。
- 建议将“市场分析已封存”这一事实同步到 README，避免新用户误解项目能力边界。

---

## 四、后续治理方案（分阶段）

| 阶段 | 方案 | 优先级 | 依赖 | 风险 |
|------|------|--------|------|------|
| A | README 入口压缩：删除/标注过时市场分析描述，精简项目结构树 | 🔴 优先 | 无 | 🟢 |
| B | 增加 `AGENTS.md`：为 AI 协作者提供项目上下文、开发规范、关键文件索引 | 🟡 并行 | 无 | 🟢 |
| C | NOTES 模板与保留：建立 `docs/NOTES/` 目录，规范临时评估文档的归档与淘汰规则 | 🟡 未来 | A 完成 | 🟡 |
| D | HISTORY/NOTES 归档：将 `docs/百尺目标板块移植评估.md` 等一次性文档归档到 `docs/archive/` 或删除 | 🟢 谨慎 | A+C 完成 | 🔴 |
| E | CHANGELOG 生成机制：建立 `CHANGELOG.md`，记录版本迭代与功能封存/重构 | 🟢 未来 | B 完成 | 🟡 |

---

## 五、人类决策拍板清单

### 技术可行性
- [ ] CHANGELOG.json 定性：本项目无 CHANGELOG，需人工记录 vs 机器校验待决策
- [ ] 硬依赖风险：无 sc-layers.js 等断言文件，README 描述性内容无硬依赖
- [ ] 历史数据不可删：`docs/百尺目标板块移植评估.md` 为一次性评估文档，可归档或删除，不涉及历史代码数据
- [ ] 版本号状态确认：当前 `2.0.0`，文档压缩不推进版本
- [ ] 安全回滚预案：README 修改可通过 Git 回滚；docs 文档删除前建议归档备份

### 业务与交付
- [ ] 接手体验 (Onboarding)：精简过时描述后 onboarding 更准确，不会恶化
- [ ] 维护成本预估 (Timebox)：A 阶段约 5-10 分钟；B 阶段约 30-60 分钟；E 阶段可 deferred
- [ ] 下游消费者影响：无外部依赖 README 的自动化脚本或 CI
- [ ] 合规与审计痕迹：建议保留 docs 归档而非直接删除，保留决策痕迹

### 决策签字

| 决策项 | 决策结果 | 决策人 | 日期 | 备注 |
|--------|----------|--------|------|------|
| 是否删除/归档 `docs/百尺目标板块移植评估.md` | | | | |
| 是否修改 README 市场分析描述 | | | | |
| 是否新增 `AGENTS.md` | | | | |
| 是否建立 `CHANGELOG.md` | | | | |

---

## 红队自我质疑

> 假设建议的迁移导致 `npm run check` 或构建失败，如何回滚？

- 本次建议只涉及文档（README.md 和 docs/ 下文件），不触及代码或构建配置。
- 回滚路径：使用 `git checkout README.md docs/` 即可恢复。
- 因此本次建议符合“最小安全第一刀”特征：改动范围小、无代码依赖、可一键回滚。
